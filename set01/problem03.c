#include <stdio.h>
int main( )
{
int a,b, sum;
printf(" Enter first Value ");
scanf("%d", &a) ;
printf(" Enter second Value ") ;
scanf("%d", &b) ;
sum = a+b;
printf(" the sum of %d and %d = %d", a,b,sum) ;
return 0;
}